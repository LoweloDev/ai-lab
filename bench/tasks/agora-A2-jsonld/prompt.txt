Security fix needed in the Next.js app under agora-web/. A debate page embeds user-authored text (title, thesis) verbatim in an inline <script type="application/ld+json"> block in agora-web/src/app/debate/[id]/page.tsx. Text containing `</script>` breaks out of the script block and injects markup into the page. There is a failing spec: run `cd agora-web && node --test tests/jsonLdScript.test.mjs`. Implement the fix so that this spec passes: create the serialization helper it expects and use it at the real call site in the debate page instead of the bare JSON.stringify. The serialized output must still round-trip through JSON.parse to deep-equal input (plain HTML-entity escaping is wrong). Do not edit the test file.
