Bug report for the web runtime adapter in runtime/web: the `runtime.describe_type` tool returns wrong paging metadata — callers that request a page of type members get a member slice and a `hasMore` flag that do not agree with `totalMembers`. One conformance test is failing. Run `cd runtime/web && node --test "test/adapter.test.js" "test/attached.test.js"` to see it. Find and fix the bug. Do not edit any test files. All 46 tests must pass when you are done.
