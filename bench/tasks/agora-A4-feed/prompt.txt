Bug report for the personalization/feed system in this Go monorepo: users report that their "Für dich" feed shows entire pages of debates about the same topic back to back — the feed used to mix topics within a page. The feed package test suite is failing. Run `cd agora-backend && go test ./internal/feed/ -count=1` to see it. Find the regression in the feed ranking/batching pipeline and fix it. The intended behavior is encoded in the tests — do not modify any test files. The whole package suite must pass when you are done.
